Order of statement execution:
1. FROM
2. WHERE
3. GROUP BY
4. HAVING
5. SELECT
6. ORDER BY


Operator prececedence:
AND and OR are both left-associative
This means that
    WHERE cgpa > 3
    AND firstName='Martin'
        OR firstName='Kathy'
turns to
    WHERE ((cgpa > 3)
    AND (firstName='Martin'))
        OR (firstName='Kathy')
